module.exports={
    GENERAL:{
        required_field:"The field {PATH} is required",
        minlength:"The field {PATH} must be at least the required minimum length.",
        maxlength:"The field {PATH} must be at most the maximum length."
    },
    USERS:{
        passwordInc:"The {PATH} must contain at least 1 lower case and 1 upper case letter"
    }
}